---
deployment:
  tasks:
   - |
      source /home/durratiz/nodevenv/repositories/durrat/18/bin/activate
      npm install --production
   - |
      source /home/durratiz/nodevenv/repositories/durrat/18/bin/activate
      npx pm2 delete durrat || true
      npx pm2 start ecosystem.config.js