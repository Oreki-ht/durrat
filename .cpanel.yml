---
deployment:
  tasks:
    - export DEPLOYPATH=/home/durratiz/public_html/
    - /bin/rm -rf $DEPLOYPATH/*
    - cd /home/durratiz/repositories/durrat
    - npm install
    - npm run build
    - npm run export
    - /bin/cp -R out/* $DEPLOYPATH
